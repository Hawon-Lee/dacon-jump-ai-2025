num_epochs: 30

device: 'cuda:0'

model_path: 'experiments/hawon_exp/250722_first_exp/model.py' # 가져올 모델 스크립트 경로
model_name: 'HawonNet' # 모델 스크립트 내에 정의된 메인 모델 이름
model_parms: # 모델 초기화할 때 넣어주는 인자들을 여기다 적어주면 됩니다.
  input_features: 384
  output_features: 1

train_data_dir: 'data/input/tr_vl' # 모델 학습용 인풋 데이터 경로
test_data_dir: 'data/input/test' # 모델 학습용 인풋 데이터 경로
data_split_file: 'data/tr_vl_split_250717.json'
batch_size: 32
num_workers: 0
criterion: 'MSELoss'

optimizer: 'AdamW'
optimizer_params:
  lr: 1e-3
  weight_decay: 1e-3
